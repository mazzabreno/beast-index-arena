# --- Dependencies (Too heavy to upload) ---
node_modules/
yarn-error.log

# --- Next.js / Frontend Build ---
.next/
out/
build/
dist/

# --- Solana / Anchor Build (Backend) ---
# 'target' contains the compiled heavy binaries
target/
# '.anchor' contains local blockchain data
.anchor/
test-ledger/

# --- Security & Secrets (NEVER UPLOAD THESE) ---
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Keypairs (Wallets/Private Keys)
id.json
*keypair.json
wallet.json
*.key

# --- OS & Editor Files ---
.DS_Store
.vscode/
.idea/
Thumbs.db
